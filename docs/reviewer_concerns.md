# eLife RP109936 — Reviewer Concerns (consolidated)

Source: https://elifesciences.org/reviewed-preprints/109936v1/reviews
eLife Assessment: **useful / incomplete**

---

## Reviewer #1

| ID | Concern | Fix |
|---|---|---|
| R1.1 | Pipeline reads as "stitching basic FIJI commands"; novelty unclear. Cite the image.sc forum example. | Reframe contribution as integrated workflow + validation evidence (R2.2/R2.3) + benchmarking section. |
| R1.2 | No comparison to Imaris, CellProfiler, CellPose, StarDist, MiNA, MitoGraph. | Add `benchmarking.py` module + comparison table in manuscript. |
| R1.3 | LLM/chatbot integration claimed but not demonstrated or instructed. | **Implement MCP server** (`automorphotrack.mcp`) + concrete usage examples; rescope text. |

## Reviewer #2

| ID | Concern | Fix |
|---|---|---|
| R2.1 | "AI-assisted / natural-language" overstated; no native interface exists. | MCP server + CLI; rewrite paragraph to reflect what AMT actually provides. |
| R2.2 | No quantitative validation against ground truth (Dice/IoU/tracking fidelity). | Expand `validation.py` with Dice, IoU, tracking-fidelity metrics + synthetic ground-truth generator. |
| R2.3 | Limited benchmarking vs CellProfiler / MiNA / MitoGraph / Mitochondria Analyzer. | New `benchmarking.py` module producing comparison-ready CSVs in those tools' conventions. |
| R2.4 | Core algorithms threshold-only; sensitive to SNR, background, bleaching, density. | Add adaptive local thresholding (Sauvola/Niblack), background-subtraction option, sensitivity-analysis helper. |
| R2.5(i) | Circularity 4πA/P² values >1 in Figs 2, 7, supp. | Fix formula in `shape_features.py` (clip at 1.0; document polygonal-discretization bias). |
| R2.5(ii) | Displacement distribution extending to negative values (Fig 3B). | Switch from KDE to bounded histograms / truncated KDE clipped at 0 in `motility.py` plots. |
| R2.5(iii) | Colocalization metrics inconsistently defined/named; Pearson r on binary masks unclarified. | Document each metric, distinguish intensity- vs mask-based, add explicit metric-definition table in `colocalization.py`. |
| R2.5(iv) | Figure legends don't match panels; N, p-values, units, assumptions under-reported. | Add per-plot annotation helper (`utils.annotate_stats`) that prints N, test name, p, effect size. |
| R2.6 | Need value-added demonstration with reduced setup burden. | Add `examples/` folder with end-to-end notebook + one-command CLI demo. |
| R2.7 | Streamline Results; reduce procedural repetition. | Manuscript revision (separate task — deferred per user). |

## Reviewer #3

| ID | Concern | Fix |
|---|---|---|
| R3.1 | **Could not install the package via any standard method.** | Fix `setup.py` + `pyproject.toml`; add CI install-test workflow; publish to TestPyPI. |
| R3.2 | AI-assisted execution not demonstrated. | Same as R1.3/R2.1 — MCP connector. |
| R3.3 | Red-green images hard for colorblind readers. | Replace all red/green pairs with colorblind-safe palettes (orange/cyan or viridis-derived). |
| R3.4 | Velocity-vs-displacement diagonal line in Fig 3C / supplemental — explain. | Add `min_detectable_displacement` discussion to `motility.py` docstring; annotate the diagonal in plots. |
| R3.5 | Use Spearman, not Pearson, for integrated correlation (non-linear relationships). | Add `method` parameter to integrated correlation, default Spearman. |
| R3.6 | Add supplementary movies. | Already generated by `*_Detection.mp4` — verify pipeline writes them by default. |
